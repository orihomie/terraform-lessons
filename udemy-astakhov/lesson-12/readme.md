In this lesson I've learned how to create local variables:<br>
```
locals {
  some_local_var = "value"
}
```

and then accessing them by writing `local.some_local_var`.<br>

Also `join(separator string, data list)` method.
